# fly.toml app configuration file generated for investment-advisor-agent
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.

app = "investment-advisor-agent"
primary_region = "sjc"

[build]
  dockerfile = "apps/agent-runtime/Dockerfile"

[env]
  PORT = "8081"
  PI_AGENT_PORT = "8081"

[http_service]
  internal_port = 8081
  force_https = true
  auto_stop_machines = "suspend"
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

  [http_service.concurrency]
    type = "requests"
    hard_limit = 25
    soft_limit = 20

[[http_service.checks]]
  interval = "15s"
  timeout = "2s"
  grace_period = "10s"
  method = "get"
  path = "/health/live"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
